{
  "name": "sparktrail.org",
  "version": "1.0.0",
  "description": "Sparktrail website",
  "scripts": {
    "build": "gulp && hugo",
    "dev": "concurrently --names \"GULP,HUGO\" -c \"red,blue\" \"gulp watch\" \"hugo server\"",
    "start": "gulp && hugo server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sparktrail/sparktrail.org.git"
  },
  "devDependencies": {
    "chalk": "^2.4.1",
    "concurrently": "^3.6.1",
    "gulp": "^4.0.0",
    "gulp-autoprefixer": "^4.1.0",
    "gulp-sourcemaps": "^2.6.4",
    "gulp-stylus": "^2.7.0"
  }
}
